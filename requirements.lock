# This file is intentionally minimal — the project uses only stdlib Python.
# Add third-party deps here as needed (e.g. Pillow for PNG output).
# Format: standard pip requirements with hashes for reproducibility.
